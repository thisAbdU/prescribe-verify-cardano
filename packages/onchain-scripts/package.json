{
  "name": "@prescribe-verify/onchain-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Aiken validator scripts for prescription verification",
  "scripts": {
    "compile": "./scripts/compile.sh",
    "check": "cd validator && aiken check",
    "build": "cd validator && aiken build",
    "deploy": "../scripts/deploy-contracts.sh"
  }
}

